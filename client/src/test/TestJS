import java.util.concurrent.TimeUnit
import org.openqa.selenium.firefox.FirefoxDriver
import org.scalatest.FlatSpec
import org.scalatest.tagobjects.FirefoxBrowser


object TestJS extends FlatSpec {


  val port = 9000
  val logUrl = "http://localhost:" + port
  val driver = new FirefoxDriver()
  "user " should "successfully hit the url" in {

    driver.get(logUrl)
    driver.manage().timeouts().implicitlyWait(10, TimeUnit.SECONDS)

  }

 /* "User" should "get the correct project name" in {
    assert(driver.findElementsByCssSelector("tr:nth-child(even)").value==="WebPage")
  }*/


}
